---
description: Arbor product milestones vs package version tags
alwaysApply: true
---

# Arbor versioning

**Product milestones** (Version 1, 2, 3 in `PROJECT.md`) describe eras of capability. **Package/git tags** (`v2.0.0`, `v2.1.0`, `v3.0.0`) track shipped releases.

## Rules

1. **`v2.0.0` = Version 2 complete** — automation and discoverability (jobs, FTS, watch, OCR, Word). The old single-column UI. Do not re-open v2 scope.

2. **Version 3 is in progress until `v3.0.0`.** Work toward Version 3 ships as **`v2.1.0`, `v2.2.0`, …** Package `v2.1.0` is the first step after Version 2. Do not publish `3.1.0`, `3.2.0`, or other `3.x` point releases. Those numbers are not used.

3. **`v3.0.0` is reserved for full Version 3 completion** — all features listed under Version 3 in `PROJECT.md` (shell, flashcards, quiz, concepts, diagrams, citations, cross-document linking, knowledge graph, embeddings, semantic search). Do not tag `v3.0.0` for the shell alone or for partial v3 delivery. Do not drop graph, embeddings, diagrams, or citations from that list to ship earlier.

4. **Version 4 starts at `v4.0.0`** when the tutor milestone is complete, with the same pattern (`v3.x` increments during v4 work if needed).

5. **CHANGELOG and package versions stay aligned** across worker, desktop, and Tauri (`pyproject.toml`, `desktop/package.json`, `Cargo.toml`).

## Current state (2026-08-26)

| Tag | Product | Notes |
|-----|---------|-------|
| `v2.0.0` | Version 2 | Shipped |
| `v2.1.0` | Version 3 (partial) | Desktop shell tagged |
| `v2.2.0` | Version 3 closeout | Package bumped. Operator tags after land. Mac E2E still open. |
| `v3.0.0` | Version 3 | **Not yet.** Tag after a recorded Mac E2E run. |

Do not backfill `v2.3.0` through `v2.8.0` for PRs #36–#41. Package `2.2.0` is Wave 8 plus that unreleased work. The operator tags `v2.2.0` after this lands.

Program: `docs/superpowers/plans/2026-08-22-v3-program/overview.md`. Closeout: `docs/superpowers/specs/2026-08-23-v3-closeout-design.md`.
